# AURA 진행 계획

> 기본 Gaussian Splat 렌더링은 완료. 이제 SuperSplat 뷰어 수준으로 렌더링 퀄리티를 끌어올리는 것이 목표

---

### Task 1. 깊이 정렬(depth sorting) 적용하기
- 매 프레임 카메라 기준 z값 계산 → splat을 뒤→앞(back-to-front)으로 정렬 → 정렬 순서대로 그리기
- 완료 기준: 뿌연(hazy) 느낌이 사라지고 표면 대비가 또렷해짐

---

### Task 2. 안티앨리어싱(cov_2d 팽창) 처리하기
- `cov_2d` 대각에 작은 값(≈0.3) 더해 픽셀보다 작은 splat 안정화 → 깜빡임/사라짐 방지
- 완료 기준: 가시·가장자리 같은 미세 splat이 안정적으로 렌더됨

---

### Task 3. SH(시점 의존 색) 적용하기
- SH 데이터를 DataTexture로 GPU 전송 → vertex에서 `texelFetch`로 읽기 → 시점 방향 기반 SH 평가로 색 보정
- 완료 기준: 보는 각도에 따라 하이라이트/광택이 변하며 색 사실감이 올라감

---

### Task 4. 렌더 파이프라인 구조 분리하기
- `Viewer.tsx`에서 Three 렌더 로직을 `RenderEngine`으로 분리 → React는 얇은 래퍼, 계약은 `GaussianData` 타입
- 완료 기준: 렌더/웹 영역이 파일 단위로 분리되어 각각 독립 수정 가능

---

### Task 5. 렌더 루프 + OrbitControls 연결하기
- 단발 `renderer.render()` → `requestAnimationFrame` 루프로 전환 → `OrbitControls`로 카메라 회전/줌
- 완료 기준: 마우스로 모델을 회전·줌하며 실시간으로 확인 가능


---

devicePixelRatio 미설정 (전체 소프트함의 1순위)
Viewer.tsx:23 — renderer.setPixelRatio(...)가 없음.

고해상도 디스플레이에서 저해상도로 렌더 후 확대 → 전체가 뿌옇게.
SuperSplat은 풀 DPR이라 선명. setPixelRatio(window.devicePixelRatio) 한 줄이 선명도를 크게 올립니다.
🟡 중간
3. fragment가 mediump
gaussian.frag:1 — precision mediump float.

가우시안 falloff(g)·색 계산 정밀도가 낮아 밴딩/거칠음이 생길 수 있음. highp로 올리면 매끄러워질 여지.
4. gl_Position.y *= -1.0 하드 플립 (correctness 냄새)
gaussian.vert:76

상하 반전을 강제로 보정하는 땜빵인데, **근본 원인(J/viewport/좌표 컨벤션)**이 어딘가 뒤집혀 있다는 신호예요.
게다가 중심점(gl_Position)만 뒤집고 공분산(cov_2d) 방향은 안 뒤집어서, 길쭉한 splat의 모양 방향이 미세하게 어긋날 수 있음. 한 번 짚어볼 가치 있음.
5. 색 관리(color space / tone mapping) 확인 필요

renderer에 outputColorSpace/toneMapping 설정이 없음. 커스텀 ShaderMaterial이라 색 공간 처리가 SuperSplat과 다를 수 있어 물 빠진 색의 원인일 가능성. (확실치 않아 검증 필요 — 이건 켜보고 비교하는 게 정확)
🟢 낮음 / 기능
6. cov 팽창(AA)이 주석 처리됨
gaussian.vert:54 — //cov_2d += mat2(0.3). 정지 화면엔 영향 작지만 움직임/줌아웃 안정성용으로 다시 켤지 결정.

7. 렌더 루프 + OrbitControls + 리사이즈 없음

단발 렌더라 회전/줌 불가 → AA·SH 효과를 평가할 방법이 없음. 창 리사이즈 대응도 없음. (Task 5)
8. 정렬 성능 (나중)

Array.sort(O(n log n)) 150만 개 — 지금은 OK, 렌더 루프 붙이면 radix sort 필요.